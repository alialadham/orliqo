import Link from "next/link";
import { Plus } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { getCampaigns } from "@/features/campaigns/data";
export default async function CampaignsPage(){const campaigns=await getCampaigns();return <div className="mx-auto max-w-[1500px] space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-3xl font-bold">Campaigns</h1><p className="mt-1 text-sm text-muted-foreground">Build, approve, schedule, and control grounded outreach.</p></div><Button asChild><Link href="/app/campaigns/new"><Plus/>New campaign</Link></Button></div><div className="overflow-hidden rounded-xl border bg-card"><div className="hidden grid-cols-[2fr_1fr_1fr_80px_100px_120px] gap-3 border-b bg-muted/40 px-4 py-3 text-xs font-semibold text-muted-foreground md:grid"><span>Campaign</span><span>Audience</span><span>Channels</span><span>Leads</span><span>Status</span><span>Activity</span></div>{campaigns.map(c=><Link key={c.id} href={`/app/campaigns/${c.id}`} className="grid gap-2 border-b p-4 transition hover:bg-muted/30 last:border-0 md:grid-cols-[2fr_1fr_1fr_80px_100px_120px] md:items-center"><div><strong>{c.name}</strong><p className="text-xs text-muted-foreground">{c.goal.replaceAll("_"," ")}</p></div><span className="text-sm">{c.audienceSource.replaceAll("_"," ")}</span><span className="text-sm capitalize">{c.channels.join(", ")||"Pending"}</span><span className="text-sm">{c.targetProspectCount}</span><Badge variant="outline" className="w-fit capitalize">{c.status.replaceAll("_"," ")}</Badge><span className="text-xs text-muted-foreground">{new Date(c.updatedAt).toLocaleDateString()}</span></Link>)}{!campaigns.length?<div className="p-10 text-center text-sm text-muted-foreground">No campaigns yet.</div>:null}</div></div>}
