import { describe, expect, it } from "vitest";
import { deterministicInterval, scheduleMessages } from "./scheduling";
describe("campaign scheduling",()=>{it("keeps deterministic intervals inside bounds",()=>{for(let i=0;i<50;i++)expect(deterministicInterval(2,6,`seed-${i}`)).toBeGreaterThanOrEqual(2);expect(deterministicInterval(2,6,"same")).toBe(deterministicInterval(2,6,"same"));});it("respects sending days, window, and daily limit",()=>{const values=scheduleMessages({startAt:new Date("2026-07-20T09:00:00Z"),count:5,days:[1,2,3,4,5],windowStart:"09:00",windowEnd:"10:00",minInterval:20,maxInterval:20,dailyLimit:2,seed:"campaign"});expect(values).toHaveLength(5);expect(values.every((date)=>[1,2,3,4,5].includes(date.getUTCDay()))).toBe(true);expect(values.reduce<Record<string,number>>((all,date)=>{const key=date.toISOString().slice(0,10);all[key]=(all[key]??0)+1;return all},{})).toEqual(expect.objectContaining({"2026-07-20":2,"2026-07-21":2,"2026-07-22":1}));});});
