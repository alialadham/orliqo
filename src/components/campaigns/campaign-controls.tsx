"use client";
import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { approveMessageAction, controlCampaignAction, dispatchDemoMessageAction, generateCampaignMessagesAction, rewriteMessageAction } from "@/features/campaigns/actions";
export function CampaignControls({campaignId,status}:{campaignId:string;status:string}){const [message,setMessage]=useState("");const [pending,start]=useTransition();const router=useRouter();const run=(fn:()=>Promise<{message:string}>)=>start(async()=>{const result=await fn();setMessage(result.message);router.refresh()});return <div className="flex flex-wrap items-center gap-2"><Button disabled={pending} onClick={()=>run(()=>generateCampaignMessagesAction(campaignId))}>Generate grounded messages</Button>{status==="paused"?<Button variant="outline" onClick={()=>run(()=>controlCampaignAction(campaignId,"resume"))}>Resume</Button>:<Button variant="outline" onClick={()=>run(()=>controlCampaignAction(campaignId,"pause"))}>Pause</Button>}<Button variant="destructive" onClick={()=>run(()=>controlCampaignAction(campaignId,"kill"))}>Kill</Button>{message?<span role="status" className="text-xs text-muted-foreground">{message}</span>:null}</div>}
export function MessageActions({id,canSend}:{id:string;canSend:boolean}){const [message,setMessage]=useState("");const [pending,start]=useTransition();const router=useRouter();const run=(fn:()=>Promise<{message:string}>)=>start(async()=>{const result=await fn();setMessage(result.message);router.refresh()});return <div><div className="flex flex-wrap gap-1"><Button size="sm" variant="outline" disabled={pending} onClick={()=>run(()=>approveMessageAction(id))}>Approve</Button><Button size="sm" variant="ghost" onClick={()=>run(()=>rewriteMessageAction(id,"shorten"))}>Shorten</Button><Button size="sm" variant="ghost" onClick={()=>run(()=>rewriteMessageAction(id,"friendly"))}>Tone</Button><Button size="sm" variant="ghost" onClick={()=>run(()=>rewriteMessageAction(id,"translate"))}>Translate</Button>{canSend?<Button size="sm" onClick={()=>run(()=>dispatchDemoMessageAction(id))}>Simulate send</Button>:null}</div>{message?<p role="status" className="mt-1 text-xs text-muted-foreground">{message}</p>:null}</div>}
