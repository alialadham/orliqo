import { readFileSync } from "node:fs";
import { describe, expect, it } from "vitest";
describe("Phase 3 database safety",()=>{const sql=readFileSync("supabase/migrations/20260722090000_phase3_atomic_queue_and_suppression.sql","utf8");it("claims only approved running due messages",()=>{expect(sql).toContain("m.approval_status = 'approved'");expect(sql).toContain("c.status = 'running'");expect(sql).toContain("m.scheduled_at <= now()");});it("enforces suppression, reply stops, and duplicate attempts atomically",()=>{expect(sql).toContain("suppression_entries");expect(sql).toContain("c.stop_on_reply");expect(sql).toContain("a.result='succeeded'");});it("keeps privileged queue functions private",()=>{expect(sql).toContain("revoke all on function private.claim_due_message");expect(sql).toContain("grant execute on function private.claim_due_message(uuid,text) to service_role");});});
