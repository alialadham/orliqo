import { expect, test, type Page } from "@playwright/test";
async function demo(page:Page){await page.goto("/login");await page.getByRole("button",{name:"Use demo workspace"}).click();await expect(page).toHaveURL(/\/app\/dashboard$/)}
test("creates a campaign through all six builder steps",async({page},testInfo)=>{test.skip(testInfo.project.name!=="desktop-chromium","Desktop builder workflow");await demo(page);await page.goto("/app/campaigns/new");await expect(page.getByRole("heading",{name:"Build safe outreach"})).toBeVisible();for(let step=0;step<5;step++)await page.getByRole("button",{name:/Continue/}).click();await expect(page.getByRole("heading",{name:"Review and save"})).toBeVisible();await page.getByRole("button",{name:"Save campaign draft"}).click();await expect(page).toHaveURL(/\/app\/campaigns\/[0-9a-f-]+$/);await expect(page.getByRole("heading",{name:"Amman growth campaign"})).toBeVisible();});
test("approves and deterministically simulates a queued message once",async({page},testInfo)=>{test.skip(testInfo.project.name!=="desktop-chromium","Desktop queue workflow");await demo(page);await page.goto("/app/queue");await expect(page.getByRole("heading",{name:"Outreach queue"})).toBeVisible();const simulate=page.getByRole("button",{name:"Simulate send"}).first();await simulate.click();await expect(page.getByText("Demo dispatch completed. No network send occurred.")).toBeVisible();await expect(page.getByText("sent",{exact:true}).first()).toBeVisible();});
test("campaign and queue remain responsive on mobile",async({page},testInfo)=>{test.skip(testInfo.project.name!=="mobile-chromium","Mobile workflow");await demo(page);await page.goto("/app/campaigns");await expect(page.getByRole("heading",{name:"Campaigns"})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false);await page.goto("/app/queue");expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth)).toBe(false);});
